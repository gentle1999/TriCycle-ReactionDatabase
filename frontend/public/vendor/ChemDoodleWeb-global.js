// The official distribution declares `ChemDoodle` with a global lexical binding.
// Expose that binding on window so Vue modules can use it without modifying vendor code.
window.ChemDoodle = ChemDoodle;
